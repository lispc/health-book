# AGENTS.md

本仓库是一本中文健康生活科普书《健康生活全书》（暂名）的书稿。

## 仓库结构

- `book/` —— 书稿正文，47 个 Markdown 文件：
  - `00-前言.md`、`01-…` 至 `38-…`（第 1–38 章，第一至七部分）、`39-…` 至 `44-…`（第 39–44 章，第八部分「常见健康问题与应急处理」）、`45-结语.md`、`46-附录.md`
  - 文件编号即章节编号，正文中的"第 N 章"交叉引用以此为准，改编号必须同步改全书引用
- `章节细纲.md` —— 全书蓝图：每章的目标、小节结构、关键事实（含数字与来源机构）、辟谣素材、行动清单方向。**写作和修改章节时以它为唯一事实依据**
- `写作规范.md` —— 全书统一的风格、章节结构、事实纪律
- `README.md` —— 目录页（含作者信息）
- `飞书发布指南.md` —— 发布到飞书知识库的方案（暂缓执行，备查）
- `scripts/add_nav.py` —— 章节导航页脚生成器（`make nav`）

## 写作约定（修改/新增章节必须遵守）

详见 `写作规范.md`，要点：

1. **章节结构固定**：场景开篇 → 为什么 → 怎么做 → 辟谣专栏（❌/✅ 格式）→ 本章行动清单
2. **篇幅**：正文章每章 2,300–2,800 字；前言/结语/附录约 2,000 字
3. **事实红线**：只使用 `章节细纲.md` 中给出的事实数字，或经联网核实（权威来源：WHO、国家卫健委、中国营养学会、专业学会指南、顶级期刊）的事实；**绝不编造数字、研究、机构名称**
4. **不确定性表述必须保留**：观察性研究说"相关"不说"导致"；细纲标注"证据弱/模型估算/类比"的地方不得写成确定结论
5. **医疗边界**：涉及诊断、用药、筛查决策时提示"遵医嘱/及时就医"
6. **风格**：通俗科普+实操（参照丁香医生语感），短句短段，不用 emoji（辟谣专栏 ❌/✅ 除外），不标题党
7. 不写参考文献列表、不署名、不写"本章完"

## 修改章节的工作流

1. 先读 `章节细纲.md` 对应章节 + `写作规范.md`
2. 修改对应 `book/` 文件
3. 若新增/删减了关键事实，**同步更新 `章节细纲.md`**，保持细纲与正文一致
4. 若改了章节结构或编号，**同步更新 `README.md` 目录、本文件、以及全书"第 N 章"交叉引用**
5. **不要手工编辑章节文件末尾的导航页脚**（`<!-- chapter-nav -->` 以下部分）——它由脚本生成；新增/删除/重命名章节后运行 `make nav` 全量重新生成
6. 完成后检查：字数在区间内、辟谣专栏与行动清单小节齐全、一级标题唯一且为 `# 第N章 标题`

## 发布

- 远程仓库：`git@github.com:lispc/health-book.git`（公开）
- 通过 GitHub Pages 发布（main 分支根目录），`index.md` 为 Pages 首页，与 `README.md` 内容保持一致——**修改目录时两个文件都要改**
- 飞书知识库发布方案见 `飞书发布指南.md`（暂缓执行）

## EPUB 构建

- `make epub` —— 用 pandoc 把 `book/*.md` 合并生成 `dist/健康生活全书.epub`（带目录，用于微信读书等导入场景）；构建时会自动剔除导航页脚
- `make nav` —— 重新生成所有章节的「上一章/返回目录/下一章」导航页脚
- `make clean` —— 删除生成的 EPUB
- `dist/` 是构建产物，不要手工编辑；改了书稿后重新 `make epub` 即可
