# 飞书发布指南（暂缓执行，备查）

> 目标：把 `book/` 下 41 章自动发布为飞书知识库（国内免翻墙、免登录可读、手机体验好、有目录树），之后改稿一条命令全量同步。
> 状态：方案已调研核实（2026-08），脚本未写。等决定启动时按本文档操作。

## 为什么选飞书而不是语雀

- 飞书：免费组织 + 自建应用即可用 API，全链路可脚本化（Markdown 导入 API + Wiki 节点 API + 权限 API），免费额度 1 万次/月
- 语雀：API Token 需付费会员（99–299 元/年）；金山/石墨/钉钉/腾讯文档的个人 API 均不可行或存疑

## 两个不能 API 化的点（官方限制）

1. **创建知识库**：`POST /wiki/v2/spaces` 只支持 user_access_token，所以知识库在飞书界面手工建一次
2. **知识库"互联网可读"**：wiki 节点不支持通过 API 设置 `anyone_readable`，在知识库设置里手工开一次（之后新增章节自动继承，无需重复设置）

## 手工配置步骤（一次性，约 15 分钟）

### 1. 创建免费组织
- 飞书客户端 → 头像 → 添加账号 → 创建新企业（免费、无需营业执照，名字随意）
- 创建后你即为超级管理员

### 2. 创建自建应用，拿凭证
- [open.feishu.cn](https://open.feishu.cn) 登录开发者后台 → 创建自建应用（如 `health-book-publisher`）
- 应用详情 → 凭证与基础信息 → 记录 **App ID**（`cli_` 开头）和 **App Secret**

### 3. 开通 API 权限（权限管理页搜索开通）
- `docs:document:import` —— Markdown 导入为云文档
- `docs:document.media:upload` —— 上传文件素材
- `wiki:wiki` —— 知识库节点操作
- `drive:drive` —— 云文档权限设置（兜底）

### 4. 发布应用
- 版本管理与发布 → 创建版本（1.0.0）→ 提交 → 自己审批通过（个人组织秒过）
- 注意：每次改权限都要重新发版才生效

### 5. 手工建知识库并设外部分享
- 飞书客户端 → 知识库 → 新建知识库（如《健康生活全书》）
- 知识库设置 → 分享设置：开启"允许分享到组织外" + "互联网上获得链接的人可阅读"
- 记下知识库链接中的 space_id（`https://xxx.feishu.cn/wiki/space/7xxxx...`）

### 6. 把应用加为知识库管理员（拉群授权法）
- 建一个群 → 群设置 → 机器人 → 添加该应用机器人
- 知识库 → 设置 → 权限设置 → 添加管理员 → 选这个群

### 7. 凭证写入 `.feishu.env`（已加入 .gitignore）
```
FEISHU_APP_ID=cli_xxxx
FEISHU_APP_SECRET=xxxx
FEISHU_WIKI_SPACE_ID=7xxxxxxxxxxxx
```

## 脚本方案（启动时实现 `scripts/publish_feishu.py`）

每章执行：`POST /drive/v1/medias/upload_all`（parent_type=ccm_import_open 上传 md）→ `POST /drive/v1/import_tasks`（file_extension=md, type=docx）→ 轮询 `GET /drive/v1/import_tasks/{ticket}` → `POST /wiki/v2/spaces/{space_id}/nodes/move_docs_to_wiki` 挂载并按章节排序。

- 认证：`POST /auth/v3/tenant_access_token/internal`（app_id + app_secret，token 约 2 小时有效）
- 更新策略：重复执行时按标题匹配已有节点，用新导入的文档替换（或先删节点再挂载）
- 全部接口均以 tenant_access_token 调用

## 参考来源

- 官方文档：open.feishu.cn（tenant_access_token、import_tasks、wiki v2、permissions）
- 关键限制依据：飞书 Wiki FAQ（创建知识库、wiki 节点链接分享限制）
